# AWS Action

Execute any and all AWS CLI commands using the aws cli, profile `{AWS_PROFILE}`, `{AWS_REGION}` region.

> **PROJECT-SPECIFIC: replace `{AWS_PROFILE}` and `{AWS_REGION}` with this project's defaults. Optionally list any asset-name prefixes/conventions that scope the project's AWS resources.**
